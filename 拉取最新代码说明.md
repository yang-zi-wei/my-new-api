# Git 多远程仓库工作流指南

## 远程仓库配置

| 名称 | 地址 | 用途 |
|------|------|------|
| `origin` | `git@github.com:yang-zi-wei/my-new-api.git` | 你的个人仓库（推送目标） |
| `upstream` | `https://github.com/QuantumNous/new-api.git` | 源项目仓库（拉取上游更新） |

## 常用操作

### 1. 首次推送到你的仓库

```bash
git push -u origin main
```

`-u` 参数会自动建立 `main` 分支与 `origin/main` 的追踪关系，后续可以直接用 `git push`。

### 2. 日常推送

```bash
git push
```

### 3. 拉取上游（源仓库）最新代码

```bash
# 获取上游所有分支的最新提交
git fetch upstream

# 将上游 main 分支的更新合并到当前分支
git merge upstream/main
```

如果你更偏好 rebase 方式（保持线性提交历史）：

```bash
git fetch upstream
git rebase upstream/main
```

### 4. 同步上游更新后推送到你的仓库

```bash
git fetch upstream
git merge upstream/main
git push
```

### 5. 在新分支上开发功能

```bash
# 基于最新的上游代码创建功能分支
git fetch upstream
git checkout -b feature/xxx upstream/main

# 开发完成后推送到你的仓库
git push -u origin feature/xxx
```

### 6. 查看远程仓库信息

```bash
# 查看所有远程仓库
git remote -v

# 查看某个远程仓库详情
git remote show origin
git remote show upstream
```

## 冲突处理

当 `git merge upstream/main` 出现冲突时：

1. 查看冲突文件：`git status`
2. 手动编辑冲突文件，解决冲突标记（`<<<<<<<`、`=======`、`>>>>>>>`）
3. 标记为已解决：`git add <冲突文件>`
4. 完成合并：`git commit`

当 `git rebase upstream/main` 出现冲突时：

1. 解决冲突文件
2. `git add <冲突文件>`
3. `git rebase --continue`
4. 如果想放弃 rebase：`git rebase --abort`

## 注意事项

- **推送**始终推到 `origin`（你的仓库）
- **拉取上游更新**始终从 `upstream` 获取
- 建议定期执行 `git fetch upstream` 保持与上游同步
- 如果要向源项目贡献代码，在 GitHub 上从你的仓库发起 Pull Request 到 upstream 仓库
