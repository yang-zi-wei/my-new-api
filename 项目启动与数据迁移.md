# 项目启动与数据迁移指南

## 一、前置要求

- 已安装 Docker 和 Docker Compose
- 项目源码已克隆到本地（包含 `Dockerfile` 和 `docker-compose.yml`）

## 二、项目启动

### 2.1 首次启动

```bash
# 进入项目目录
cd /path/to/new-api

# 构建镜像并后台启动所有服务
docker compose up -d --build
```

`--build` 会根据本地 `Dockerfile` 构建应用镜像。启动后会运行三个容器：

| 容器 | 说明 | 端口 |
|------|------|------|
| `new-api` | 应用主服务 | 3000 |
| `postgres` | PostgreSQL 数据库 | 内部 5432 |
| `redis` | Redis 缓存 | 内部 6379 |

启动完成后浏览器访问 `http://<服务器IP>:3000` 即可使用。

### 2.2 查看服务状态

```bash
# 查看所有容器运行状态
docker compose ps

# 查看应用日志（实时跟踪）
docker compose logs -f new-api

# 查看所有服务日志
docker compose logs -f

# 查看最近 100 行日志
docker compose logs --tail 100 new-api
```

## 三、项目重启

### 3.1 重启所有服务（无代码变更）

```bash
docker compose restart
```

### 3.2 仅重启应用（不重启数据库和 Redis）

```bash
docker compose restart new-api
```

### 3.3 停止后重新启动

```bash
# 停止所有服务（保留数据）
docker compose down

# 重新启动
docker compose up -d
```

### 3.4 代码修改后重新部署

本地代码修改后必须加 `--build` 重新构建镜像，否则改动不会生效：

```bash
docker compose up -d --build
```

Docker 有构建缓存机制，只有变更的层会重新构建，未修改的部分会复用缓存。

### 3.5 强制完全重新构建（排查构建缓存问题时使用）

```bash
docker compose build --no-cache && docker compose up -d
```

## 四、拉取上游更新后重新部署

```bash
# 1. 拉取上游最新代码
git fetch upstream
git merge upstream/main

# 2. 重新构建并启动
docker compose up -d --build
```

## 五、数据说明

项目运行时产生的数据存储在以下位置：

| 数据 | 存储位置 | 说明 |
|------|----------|------|
| PostgreSQL 数据 | Docker 命名卷 `new-api_pg_data` | 数据库全部数据 |
| 应用数据 | `./data` 目录（项目根目录下） | 应用上传文件等 |
| 应用日志 | `./logs` 目录（项目根目录下） | 运行日志 |

查看命名卷的实际路径：

```bash
docker volume inspect new-api_pg_data
```

## 六、数据迁移到新服务器

### 方案一：导出数据库 + 复制文件（推荐）

适用于同类型数据库之间的迁移，步骤清晰，可靠性高。

#### 第一步：在旧服务器上导出数据

```bash
# 1. 导出 PostgreSQL 数据库
docker exec postgres pg_dump -U root -d new-api > backup.sql

# 2. 打包应用数据和日志
tar -czf app-data.tar.gz data/ logs/
```

#### 第二步：在旧服务器上将项目源码推送到 Git 仓库

```bash
git add . && git commit -m "latest changes"
git push origin main
```

#### 第三步：传输数据文件到新服务器

```bash
# 通过 scp 传输
scp backup.sql app-data.tar.gz user@新服务器IP:/path/to/new-api/
```

或者用 rsync：

```bash
rsync -avz backup.sql app-data.tar.gz user@新服务器IP:/path/to/new-api/
```

#### 第四步：在新服务器上恢复

```bash
# 1. 克隆项目源码
git clone git@github.com:yang-zi-wei/my-new-api.git /path/to/new-api
cd /path/to/new-api

# 2. 解压应用数据
tar -xzf app-data.tar.gz

# 3. 先构建并启动数据库和 Redis（不启动应用）
docker compose up -d postgres redis

# 4. 等待 PostgreSQL 完全就绪
sleep 10

# 5. 导入数据库
docker exec -i postgres psql -U root -d new-api < backup.sql

# 6. 构建并启动应用
docker compose up -d --build

# 7. 验证服务是否正常
docker compose ps
```

#### 第五步：验证

```bash
# 检查健康状态
curl http://localhost:3000/api/status

# 查看日志确认无报错
docker compose logs -f new-api
```

---

### 方案二：直接迁移 Docker 卷（快速）

适用于希望快速搬迁、不需要改动数据库类型的场景。

#### 第一步：在旧服务器上停止服务并打包

```bash
# 1. 停止所有服务
docker compose down

# 2. 找到卷的实际存储路径
docker volume inspect new-api_pg_data --format '{{.Mountpoint}}'
# 通常输出：/var/lib/docker/volumes/new-api_pg_data/_data

# 3. 打包数据库卷（需要 sudo）
sudo tar -czf pg-volume.tar.gz -C /var/lib/docker/volumes/new-api_pg_data/_data .

# 4. 打包应用数据
tar -czf app-data.tar.gz data/ logs/
```

#### 第二步：传输到新服务器

```bash
scp pg-volume.tar.gz app-data.tar.gz user@新服务器IP:/path/to/new-api/
```

#### 第三步：在新服务器上恢复

```bash
# 1. 克隆项目源码
git clone git@github.com:yang-zi-wei/my-new-api.git /path/to/new-api
cd /path/to/new-api

# 2. 解压应用数据
tar -xzf app-data.tar.gz

# 3. 先创建卷（只启动 postgres 使其自动创建卷，然后立即停止）
docker compose up -d postgres
docker compose down

# 4. 用备份数据覆盖卷内容（需要 sudo）
VOLUME_PATH=$(docker volume inspect new-api_pg_data --format '{{.Mountpoint}}')
sudo rm -rf ${VOLUME_PATH}/*
sudo tar -xzf pg-volume.tar.gz -C ${VOLUME_PATH}

# 5. 构建并启动所有服务
docker compose up -d --build
```

---

### 方案三：使用 MySQL 时的迁移

如果你使用的是 MySQL 而非 PostgreSQL，导出和导入命令有所不同：

```bash
# 导出（旧服务器）
docker exec mysql mysqldump -u root -p123456 new-api > backup.sql

# 导入（新服务器，先启动 mysql 服务）
docker exec -i mysql mysql -u root -p123456 new-api < backup.sql
```

其余文件传输和操作步骤与方案一相同。

## 七、常见问题

### Q: 代码改了但启动后没生效？

必须加 `--build` 参数重新构建镜像：

```bash
docker compose up -d --build
```

不加 `--build` 会复用旧镜像，本地代码改动不会被包含进去。

### Q: 启动后无法访问页面？

```bash
# 检查容器是否正常运行
docker compose ps

# 查看应用日志排查错误
docker compose logs new-api

# 确认端口未被占用
lsof -i :3000
```

### Q: 数据库连接失败？

```bash
# 确认 postgres 容器正在运行
docker compose ps postgres

# 查看 postgres 日志
docker compose logs postgres

# 手动测试连接
docker exec -it postgres psql -U root -d new-api -c "SELECT 1"
```

### Q: 如何修改数据库密码？

1. 修改 `docker-compose.yml` 中 `POSTGRES_PASSWORD` 和 `SQL_DSN` 里的密码，保持一致
2. 如果数据库已有数据，还需进入数据库手动修改密码：
   ```bash
   docker exec -it postgres psql -U root -c "ALTER USER root PASSWORD '新密码';"
   ```
3. 重启服务：`docker compose restart`

### Q: 构建很慢怎么办？

- Docker 有层缓存机制，只有修改过的步骤及之后的步骤会重新执行
- 如果依赖没有变化（`go.mod`/`package.json` 未改），依赖安装步骤会命中缓存
- 避免使用 `--no-cache` 除非确实需要完全重建

### Q: 迁移后数据丢失？

- 确认 `backup.sql` 文件大小合理（不是空文件）
- 确认导入时数据库已完全启动就绪
- 检查导入命令的输出是否有报错

### Q: 如何完全清除数据重新开始？

```bash
# 停止并删除所有容器和数据卷（不可恢复！）
docker compose down -v

# 删除本地数据目录
rm -rf data/ logs/

# 重新构建并启动（全新安装）
docker compose up -d --build
```
